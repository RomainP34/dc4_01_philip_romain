/*Écrire une fonction qui affiche l’alphabet en majuscule, une string
contenant l’alphabet en minuscule sera passée en paramètre de la
fonction. Chaque lettre devra être stockée dans une cellule du
tableau.*/

var alphabet = ('abcdefghijklmonpqrstuvwxyz');

Array.from(alphabet);

console.log(Array.from(alphabet));